data = [];
reference = [];
{{for index,score in scores}}
  {{if score.data != null}}
    data['score{{index}}']= {{json(score.data)}};
  {{end}}
  {{if score.referenceData != null}}
    reference['score{{index}}']= {{json(score.referenceData)}};
  {{end}}
{{end}}

samples = {{json(samples)}};
